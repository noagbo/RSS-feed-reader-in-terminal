RSpec.describe RSS::FEED::READER::IN::TERMINAL do
  it "has a version number" do
    expect(RSS::FEED::READER::IN::TERMINAL::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
